name: Keep Hugging Face Space Awake

on:
  schedule:
    # This runs every 24 hours at midnight UTC
    - cron: '0 0 * * *'
  workflow_dispatch: # Allows you to run it manually to test

jobs:
  ping_space:
    runs-on: ubuntu-latest
    steps:
      - name: Send Request to HF Space
        run: |
          curl -I https://saqibmurtaza-chainlit-shopping-agent.hf.space
